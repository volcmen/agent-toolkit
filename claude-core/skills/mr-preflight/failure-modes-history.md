# Failure-modes history (retro-facing)

Append-only provenance for the rows in `failure-modes.md`. Written by review-retro; never read by the gate or pasted into briefs. 2026-09-02: 29 rows; compression candidates F12→F3, F8→F6, F14→F16. Each row = a class of defect that survived my own review and was caught by a human.
`recurrences` counts times a class recurred AFTER being enforced by the gate — any recurrence means the
detection question is not mechanical enough and must be rewritten, not re-read.

| id | class | detection question (mechanical) | origin | recurrences |
|----|-------|--------------------------------|--------|-------------|
| F1 | contract-change-unconsumed | Diff changes an exported signature/return/shape? → grep EVERY call site; table each as consumes/indifferent-with-proof. A fix visible only in tests = FAIL. | !16327 B2 (5 callers discarded new boolean) | 0 |
| F2 | empty-fallback-as-success | Any new/changed catch/except/fallback branch? → (a) what literal does it return ({} [] None '' '-' 0) and does the consumer render it as pass/clean? (b) is the caught type broader than the one expected failure (bare Exception, catch-all)? List every cause the clause swallows: intended vs also-swallowed; unmatched causes must re-raise. (c) if the fallback literal feeds a value CONSUMED BY MORE THAN ONE caller (shared filter / allow-list / exclusion set), grep every consumer and state the direction of error per consumer — an empty set that only under-reports for consumer A can over-report for consumer B (inverted checks); "safe because it only under-reports" without a per-consumer table = FAIL. | !16340 B4; NTD-4064 ×2; devops !1620 (catch Exception ate build-abort → silent no-SSO shard); !16321 (AssertionError→[] fed both in_opt↔rings inverted checks — clause (c) authored from this finding); 2026-08-30 sweep ×3: !16345 (`{}` counted as recovery success), !16222 (bare except), !16344 ('-' NO_ALERT filtered like PASS in save dialog — clause (c) consumer table not built) | 5 |
| F3 | fix-composition-hazard | List MY other open MRs touching the same event surface / field / save path. For each intersection: state the composed behavior and test it. | !16339×!16223 M1 (blur-guard × clamp-on-blur = silent corruption); 2026-08-30 sweep ×4: !16341/!16324 (MUI barrel × !16517 sweep), !16173 (localStorage .ts test × !16517 node project), !16327×!16173 (same hunk). ATTRIBUTION: gate not run across the batch — run `glab mr list --author=@me` and diff-intersect files on every push | 4 |
| F4 | blast-radius-unmeasured | Change alters behavior for a population (configs, CUs, fields, users)? → count it or name the bound. "Unknown population" on a blocking/visible change = FAIL (split or measure). | !16173 B1 (orphan count unknown); !16323 M2 (every unset field); !16344 M6 (every standalone CU) | 0 |
| F5 | sibling-path-unswept | Fixed a pattern in one path? → grep for structural siblings (same guard shape, same finally, same import). Fix or justify EACH by name. | !16225 M4 (2 of 3 finallys guarded); CUConfigEditor 3-save-paths history; 2026-08-30 sweep ×6: !16344 (sibling corridor row + 2nd corridor_policies consumer), !16341 (render-layer lock, preset/CCF apply paths bypass), !16319 (mergeField save path + validateOverlaps path), !16327 (1 of 4 guards tested), !16355 (2nd requirements file), !16173 (2 overlap reconstructions). ATTRIBUTION: reasoning without artifact. FORM FIX: paste the `rg` of the structural sibling shape with every hit tabled fixed|justified | 6 |
| F6 | boundary-uncoerced | New comparison/lookup/ORDERING on data crossing JSON/API boundary? → coerce both sides; garbage tolerated without raise. Grep the diff for `sorted(`, `min(`, `max(`, `<`, `>` over values sourced from a config/API dict: any call whose iterable can hold mixed int/float/str (overlap ids, phase ids, unit ids) without an explicit `key=str`/`key=canonical_*` = FAIL. | !16344/!16345 m7 (string ids from MOS); !16221 (sorted() over mixed-type dangling overlap ids → TypeError kills the whole CCV batch) | 1 |
| F7 | non-discriminating-test | For each new test: name the mutation that makes it fail. Actually run one mutation (red), revert (green). Test that passes against broken impl = no test. | !16222 m9 (empty fixture); NTD-5834 mock artifact; 2026-08-30 sweep ×7: !16414 (asserted double's verdict), !16344 (2/4 survived mutation), !16327 (spread mock dropped prototype methods → validation never ran), !16324 (reopen test blind to prop change), !16323 (headline numericDraft untested), !16345 (user-visible change untested), !16110 (global random.seed(42) leak made property tests replayable). ATTRIBUTION: question answered without evidence — 'mutation verified' was asserted, not pasted. FORM FIX: the gate output must include the red run's failing assertion line verbatim per new test; no pasted red line = FAIL | 7 |
| F8 | partial-function-misuse | Diff adds parseInt/parseFloat/JSON.stringify/Number on external input? → justify prefix-parse ("12abc"→12), undefined-return, "1e5" cases explicitly. | !16319 M3; !16330 m8; !16323 parseInt find; !16323 (clearing dictionary numeric commits '') | 1 |
| F9 | scope-rider | Diff hunk not required by the ticket (dep pin, extra field lock, refactor)? → split to own MR or name it in the description with rationale. | !16331 m11 (hypothesis/faker); !16341 forbidden_transitions | 0 |
| F10 | remediation-path-removed | Removing/locking an affordance? → grep for every flow that routes users TO it (checks, links, buttons). Each must have a surviving path. | !16341 M5 (locked field vs blocking CCV checks); !16283 (after the guard, Jenkins UI has no tables-preserving trigger) | 1 |
| F11 | hand-copied-mirror | For each ADDED function that computes a verdict/normalisation (predicate, canonicaliser, guard): pick its distinctive literal(s) (`'pplt_fya'`, a regex, a magic constant) and `rg <literal> --type py -l` (or ts) across the repo EXCLUDING the diff. Any hit in a shared/production module = the new function must import/delegate to it, or the extraction ticket is filed and linked — a paraphrase kept "because it's just a tool" = FAIL. Reasoning without the rg output pasted = FAIL. | !16222/!16343/!16173/!16328 pattern; NTD-6691 six FYA predicates; !16110 is_fya paraphrase of Overlap.is_fya survived to head, caught by user DRY ask not the gate (fixed f37c873b) | 1 |
| F12 | workaround-divergence | Diff adds an env/test workaround (Node flags, shims)? → is there a canonical mechanism in another branch/MR? Use it or align on one. | S3 (three localStorage mechanisms across MRs); !16173 (--no-experimental-webstorage no-op on Node 22, fatal on Node 20) | 1 |
| F13 | recovery-branch-assumption | Diff makes an error-recovery/idempotency branch reachable or newly load-bearing? → prove its trigger matches the real dependency's behavior: doc citation, dependency source read, or live probe — pasted. Grep the diff for status/message string-matching (`"HTTP 409" in`, `.includes('404')`); each match on a newly-exercised path needs provenance or FAIL. | devops !1620 (Keycloak dup mapper = 400, code matched "HTTP 409"; branch was dead until this MR); !16345 (recovery re-ran the identical failed lookup — branch could never engage) | 1 |
| F14 | untraced-runbook | Diff adds/changes an operational procedure (doc command, runbook, script invocation)? → trace the exact command with the REAL producer of its inputs; state its effect on existing infra (creates/mutates/destroys what, by name). Conceptual walkthrough without the input-producer trace = FAIL. | devops !1620 (backfill doc + build_bootstrap_plan → provisions a brand-new shard); !16283 (README documented the failure, not the recovery) | 1 |
| F16 | description-claim-drift | For each behavioral claim in the MR description / commit body (idempotent, rejects X, converges, backwards-compatible, fixes Y): name the code line AND test that makes it true — claim without artifact = FAIL. Sweep the WHOLE standing description on every gate run, not only sentences about the current diff — pre-existing claims rot (a "save-blocking" claim survived a passing F16 while the check had been downgraded to FYI by another MR); any claim about ANOTHER system's behavior (a gate, a severity class, a consumer) needs that system's current source read on this run, cited file:line. Reverse sweep: behavior the diff demonstrably fixes/changes but the description omits → FAIL until claimed. RE-RUN TRIGGER: this sweep fires on EVERY push to an MR already declared ready or under review, not only at first ready-declaration — an amending commit that changes the mechanism (`git log --oneline <last-gated-sha>..HEAD` non-empty = re-sweep) invalidates prior F16 passes. | Alon on !16328 (unclaimed fixed bug); !16319 M3 (comment promised verbatim-keep, code parseFloat'd); !1620 doc ("every step idempotent … 409" false for mappers); !16339 (description claimed blur, amending commit 7773cccf switched to preventDefault post-gate; aviv caught the contradiction + the scroll-trap regression); 2026-08-30 sweep ×3: !16447 (Step 3 pill still said 'blocking' after FYI downgrade — UI copy is a claim), !16341 (note routed operators to wrong editor), !16331 ('confirmed against real MOS?' — verification claim without artifact) | 4 |
| F17 | slop-comment-in-source | Run `git diff <target>...HEAD -U0 -- ':!*.md' \| rg '^\+' \| rg '\b(NTD\|NTST\|NTQA)-[0-9]+\|![0-9]{4,}'` — any hit in source (comment, docstring, string, filename) = FAIL. Then COUNT added comment/docstring lines mechanically: `bash ~/.claude/hooks/f17-comment-count.sh <target>` — it prints one line per surviving added comment after the toolchain allow-list (pragma, shebang, lint directive, licence header, public-library API docstring). Covers hash-family (py/sh/yml/tf/toml/rb) and slash-family (ts/tsx/js/css/groovy/java/kt/go/rs/c/cpp/swift), LINE-LEADING **and** TRAILING comments (`sleep(2)  # grace...`), with string-literal stripping so a `#` inside a string and a `https://` URL are not hits. Validated 2026-08-18: 0 false positives across 23 cleaned branches; trailing-comment blindness had let real hits through on !16345 (5 lines) and !839 (1 line). Non-zero output = FAIL, one line at a time, no aggregate judgement; rationale moves to commit body/MR description. FLEET TRIGGER: this class is not per-branch — when it recurs, re-run both halves across EVERY open MR of mine (`glab mr list --author=@me`), not only the branch in hand. | NTD-6686 audit test (module docstring + 15 inline comments w/ ticket keys); ticket-key half promoted to PostToolUse hook `~/.claude/hooks/f17-ticket-keys.sh` 2026-08-18; aviv.galmidi 2026-08-18 re-reported the SAME class on !16343 !16328 !16329 !16321 !16110 with 16 of 24 open MRs carrying ticket keys in source | 1 |
| F18 | test-harness-convention-mismatch | New/changed test file? → open 2+ sibling test files in the same directory; table base class, assert style, fixture mechanism (unittest setUp vs pytest fixtures), runner invocation. Any axis where the new file diverges from the siblings without a named repo reason = FAIL. | NTD-6686 audit test (bare pytest classes + tmp_path vs 130 BaseTestCase siblings); 2026-08-18 I injected a repo-WIDE "must be unittest.TestCase" claim into worker briefs, which overrode the sibling check and made !16340 and !16344 disagree inside one directory. The rule is documented and PATH-SCOPED — repo CLAUDE.md says "Python tests are located in `dev/tests/`. Write them as `unittest.TestCase`", so it governs `dev/tests/**` ONLY; anything outside that path (e.g. `dev/tools/...`) follows its own siblings. Verified: `dev/tests/unit_tests/**` = unittest/BaseTestCase (unittest discovery); `dev/tools/ntstui_react/backend/tests/` = bare pytest, own conftest.py + src/notraffic/pytest.ini, unittest discovery collects 0 there. Third data point 2026-08-18: mos-backend runs NO unittest discovery at all (`tasks/test.py` -> `python -m pytest`, pyproject pins pytest==6.2.4); its siblings are bare pytest classes with plain `assert`. Three repos, three answers — which is the whole point of the rule. Never state a global harness rule in a brief — the siblings ARE the rule. | 1 |
| F19 | example-only-test-for-contract-function | New test targets a pure/contract-bearing function (parser, canonicaliser, predicate, serialiser, normaliser)? → `rg 'hypothesis\|fast-check\|@given\|fc\.'` the test file. Example-only = name the testing.md property class considered (round-trip/invariant/idempotence/oracle/metamorphic) and why none applies — no named property = FAIL. Also `rg 'derandomize|Faker\.seed|seed_instance|seed\s*[=:]'` on added tests: any pinned seed = FAIL. DISGUISED-KEY TELL: a seed/id/magic literal whose digits equal the ticket number (`seed_instance(6486)` on NTD-6486, LCG `seed = 6570` on NTD-6570) is BOTH a pinned seed and a ticket reference that defeats the F17 `NTD-` grep — grep the diff for the bare 4-digit ticket number as a numeric literal, not just for the `NTD-` prefix. | NTD-6686 audit test (canonical_overlap_id/is_fya = pure contract fns, example-only) | 0 |
| F15 | validator-intent-mismatch | New validation/normalization guard? → (a) grep the guard for computed-but-discarded results (`.strip()` `.trim()` `.lower()` checked but raw value stored); (b) enumerate inputs violating the guard's STATED purpose (its own comment/doc) — wildcards (*), empty, whitespace, comma-lists — and show each rejected or normalized in a test. | devops !1620 (strip() discarded → " notraffic.com" never matches; `*` sails past a blank-check whose comment says "any account"); 2026-08-30 sweep ×4: !16414 (refs/heads/x passes check-ref-format then resolves to nothing; tail-glob decoy), !16223 ×2 (isSpeedOutOfRange('') false → 0 persisted), !16319 (blank-ish outside literal list) | 4 |
| F20 | capability-duplicates-existing-validator | Diff adds a new script/tool/check that asserts a contract? → grep the repo for an existing validator over the SAME contract (`rg -l '<the key/field being checked>'` across validators, then read the hits). For each overlapping check, state redundant | distinct-and-why in a table IN THE MR DESCRIPTION before requesting review. ALSO state the tool's negative space: each adjacent contract class it deliberately does NOT check, with the one-line reason (already save-blocked elsewhere / no writer produces it) — a reviewer asking "why doesn't it check X?" = FAIL. Overlap discovered by the reviewer = FAIL. | !16110 (overlap audit's FYA check duplicated CCV `_check_lane_to_overlaps_configured_correctly`; reviewer moved to close the MR); !16110 alon "why only FYA, what about vehicle overlaps" — negative space absent from description | 0 |
| F21 | session-link-leak | Any outbound text (MR/PR description, commit body, Jira, Confluence, Slack, review comment) about to be created or updated: `rg -n 'claude\.ai/code/session_|Claude-Session:|Generated with \[?Claude|Co-Authored-By: Claude'` over the exact payload — any hit = FAIL, strip it before sending. Also re-read the STANDING MR description on every gate run, not only text added this round; footers land silently on the first push and persist. These URLs are private to my machine and resolve for nobody else. | user callout 2026-08-24 (session URL in a GitLab MR description) | 0 |
| F22 | test-unwired-from-runner | New/changed test file? → name the runner that executes it in CI (job + discovery glob) and paste the line: `rg -n '<dir or glob>' .gitlab-ci.yml .gitlab/ci src/jenkins-cloud` or the Jenkins job's test-list. No hit = the test rots silently → wire it, or state the gap in the MR description with the ticket that will. | !16414 (groovy test nothing runs); !16345/!16344 (ntstui backend pytest has no CI); !16222 (dash module no runner) — all caught by reviewer 2026-08-30; RECURRED NTD-7802 2026-09-01: test_cu_config_validation.py grew across two merged MRs (e47d1a9c40, 7f7161d044) still wired to no job — the disclose-the-gap escape was taken with no artifact; escape now requires pasting an OPEN ticket/MR URL that wires it, prose disclosure alone = FAIL | 1 |
| F23 | paginated-response-first-page-only | Diff reads a list from an API? → `rg -n '"next"\|\.next\b\|page=' ` the client module; if the endpoint paginates (read its DRF/OpenAPI source), the loop must follow `next` or pass a filter the server actually honours (read the ViewSet filter_backends — `id=` was NOT honoured on /admin/units/). Shape-handling without content-following = FAIL. | !16345 (only page 1 of /api/v2/admin/units/ searched) | 0 |
| F24 | dead-code-left-by-fix | After the fix, run the repo's dead-code signal over touched files (ts: `npx tsc --noUnusedLocals`/eslint no-unused-vars; py: `ruff --select F401,F841`/vulture) AND read each touched function for a block whose result the new code no longer consumes (copy-then-pop, fake-timer scaffolding, renamed-but-kept helper). Every leftover = delete or justify by name. | !16331 (copy-then-pop dead after schema fix); !16324 (renderDialog fake-timer dance dead) | 0 |
| F25 | feedback-surface-occluded | Diff adds/moves a user-facing error, toast, or rejection banner? → trace where it renders relative to the element that triggered it (modal z-order, drawer, unmounted parent). A rejection rendered behind/under/after-unmount of its trigger = FAIL; test must assert the message is visible (`toBeVisible`), not merely present. | !16327 ('Save & Leave' rejection rendered behind the modal that triggered it) | 0 |
| F26 | merge-without-required-approval | BEFORE any merge (not at ready-declaration): `glab api projects/<id>/merge_requests/<iid>/approvals` + the MR's reviewer list — table every listed reviewer as approved / explicitly-waived-in-thread; any reviewer neither approved nor waived = FAIL, do not merge. Component-owner surfaces (NOC-tools → aviv.galmidi) require the OWNER's row to be 'approved', other approvals do not substitute. | !16474 (merged with aviv listed reviewer, un-reviewed; owner demanded revert-level escalation post-merge) | 0 |
| F27 | live-drift-without-tracking-artifact | MR description/retest names any live-only state (kubectl set env, admin-API change, Jenkins out-of-band apply, closed-unmerged sibling MR a fix depends on)? → table each drift item with its durable artifact: an OPEN MR or ticket URL, pasted. Disclosure without a linked open artifact = FAIL — 'documented in the description' does not survive a redeploy; a CLOSED MR is not an artifact. | !16474 (dev143 env var + closed !1645 CSP both disclosed but untracked; owner escalated both as blockers) | 0 |
| F28 | inherited-diagnosis-unverified | Diff's premise comes from a diagnosis not proven against the live boundary this run — someone else's (ticket comment, review note, prior MR body) OR my own code-reading-only theory — and is about to be committed, shipped, or POSTED externally (ticket comment, Slack, MR body)? → (a) for EVERY symbol, module, attribute or file the diagnosis names, run `rg -n '<symbol>' <repo>` and paste the hit count — zero hits on the load-bearing symbol = FAIL, the premise is fiction; (b) if the diagnosis asserts runtime behavior of a boundary (import system, descriptor/registry, clock, filesystem, network, GC), reproduce it against the REAL boundary and paste the transcript — a test that mocks that boundary cannot observe the subject and does not count as evidence; (c) state the mechanism in one sentence and name the artifact (grep output or probe transcript) that proves it. Restating an upstream comment = FAIL; posting my own untested boundary theory as a root-cause claim = FAIL. | NTD-7802 2026-09-01: my ticket comment asserted a CORS-stripped-401 mechanism from reading pythonApi.ts alone; live curl showed the 401 carries ACAO:* and the real mechanism was upstream worker starvation. NTD-6465 !16592/!16593: `RedisApiHandler` (the attribute the whole fix targeted) has ZERO hits repo-wide and `redis_api_handler/__init__.py` is 0 bytes, so the `sys.modules` husk cannot exist; premise inherited unchecked from a ticket comment into commit body, MR description and a test constant. Real mechanism (protobuf global descriptor pool / a fully-initialized cached leaf module) found only by probing a live interpreter. All 9 tests passed and were mutation-checked because they mocked `__import__` — the mocks made the false premise untestable. Joel (QA) 2026-08-31 | 0 |
| F29 | acceptance-item-unmet-at-closure | Before moving a ticket to Ready for Validation / Resolved, or writing `Closes <KEY>`: fetch the ticket and identify the CURRENT authoritative acceptance list (a rescoping comment supersedes the description — use the newest one, not the field). Table EVERY item as delivered (cite the code line AND the test) | not-delivered | withdrawn (quote the comment that withdrew it). Any item without one of those three states, or a `Closes` on a ticket with a not-delivered item = FAIL. Also verify the delivered items were delivered AS WRITTEN: an item satisfied by an opposite mechanism (spec said evict, code retries) is not-delivered-plus-substitution and must be stated in the MR description. | NTD-6465 !16340: shipped as "self-heal … import failure", ticket moved to Ready for Validation with resolution Done, but acceptance item 1 (evict the husk) was never implemented and item 1's spec was in direct tension with the retry that shipped; QA's skeptic pass caught it six weeks later. Joel (QA) 2026-08-31 | 0 |
| F30 | remote-call-volume-invisible-in-tests | Diff adds or newly enables an outbound call inside a per-item/per-request path? → `git diff -U0 | rg '^\+.*(client\.(get|post)|requests\.(get|post)|httpx)'`; for each hit, state calls-per-user-action (N CUs × M endpoints × retries) and the upstream's concurrency budget, and add a test asserting an upper bound on transport `call_count` for one action. Tests that mock the transport without a call-count bound = FAIL — mocks hide cost, not just behavior. | NTD-6059 e47d1a9c40: coercion fix silently enabled one IM proxy call per CU (plus a 1.4MB full units fetch per CU); fully-mocked tests passed while the call pinned MOS gunicorn workers 30s each → fleet-wide CCV outage. Eliel/Joel 2026-09-01 | 0 |
| F31 | client-timeout-claimed-as-upstream-protection | Fix adds `asyncio.wait_for`/`timeout=` around an outbound call and the MR claims it fixes upstream saturation/outage? → answer in the MR description: what happens to the ABANDONED request server-side (paste the upstream worker/timeout config lines); if the upstream worker keeps running after client abandon, a client cap is not a fix — require source-side suppression (backoff/circuit breaker/filtered query) or an upstream timeout < the upstream's kill limit. Claim without the server-side answer = FAIL. | 7f7161d044: 5s client cap shipped as the NTD-7802 fix; MOS worker still blocked 30s per abandoned IM request until gunicorn SIGKILL, outage continued; bisect by teammates found it same day | 0 |
| F32 | mr-state-hygiene (PROMOTED TO SCRIPT — no gate prose) | Origin 2026-09-03, !16490. The MR was re-targeted from the superseded `ntst_7.0.0` to `main`; GitLab kept the pre-retarget `diff_refs.start_sha` (`67a247a1`, the old release tip) and never recomputed the merge base, so Changes/Commits rendered 737 files and 700+ commits for a branch that was 1 commit / 1 file ahead of `main`. Neither I nor the reviewer questioned the number for a day — the noise was attributed to the branch, not to GitLab's cached refs. Detection is two deterministic invariants, no model reasoning: `diff_refs.base_sha == git merge-base <target> <head>`, and API `changes_count` ≈ `git diff --name-only <merge-base> <head> | wc -l`. Repair is one read-write call, `PUT /merge_requests/:iid/rebase`, which forces recomputation. RESOLUTION: all 11 mechanical MR-metadata invariants moved into `mr-doctor.sh` (failures-only output, one PASS line otherwise, read-only — it prints the repair command and never rebases on its own, because rebase mutates SHAs, re-triggers CI and can drop approvals). Only the one-line F32 pointer row is gate-visible; expanding these into prose rows would re-create the token cost the script exists to remove. Consulted externally on placement 2026-09-03: separate script from `preflight-triage.sh` (diff-content heuristics) because the class is MR state, not source content, and it is testable without a diff. Policy going forward: any rule that is a stable yes/no over reliably-fetchable inputs is implemented in a script and DELETED from `failure-modes.md`; only rules needing semantic judgement stay prompt-visible. |
